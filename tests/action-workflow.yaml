name: Test Local Action Outside Standard GitHub Workflows Directory
on: [push]

jobs:
  test:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout code
        uses: actions/checkout@v7
      - name: Test Release Action
        uses: ./
        with:
          release_type: 'minor'
          push_changes: 'false'